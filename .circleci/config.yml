version: 2.1

workflows:
  version: 2
  build:
    jobs:
      - build

jobs:
  build:
    working_directory: ~/network
    docker:
      - image: circleci/node:14.15.4
    steps:
      - run:
          name: version
          command: node -v
      - run:
          name: install
          command: npm install
      - run:
          name: build
          command: npm run build
